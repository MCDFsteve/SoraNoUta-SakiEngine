name: soranouta_project
description: "SoraNoUta project-level module package"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.1.0
  sakiengine:
    path: ../../../Engine

flutter:
  uses-material-design: true
