# 第二章 Ren'Py → SakiEngine 演出还原记录

## 还原原则

- 原稿：`/Library/Afolder/RenpyProject/SoraNoUta/game/code/story/chapter2`
- 目标：`GameScript/labels/cp2_001.sks`—`cp2_019.sks`
- 台词和已经优化过的剧情结构以当前 SakiEngine 剧本为准。
- ADV/NVL 身份按 Ren'Py 角色定义迁移：只有
  `Character(..., kind=nvl)` 的台词进入 SakiEngine `nvlm`；
  `nvl clear` 仅拆分累计页面，不作为进入 NVL 的指令。
- 背景、CG、立绘、表情、站位、进退场、镜头动画、画面震动、
  音乐、环境音、音效和停顿逐个从 Ren'Py 原稿对齐。
- 自动还原的节点带有 `renpy-performance` 和原稿行号标记，可以回查，
  演出节点和 NVL 边界都可以由
  `tools/sync_chapter2_performance.py` 确定性重建。
- 原稿中的 `xiayo pose4` 属于第一章废案侧身立绘，不导入第二章；
  对应的思考演出统一改用稳定的 `xiayo2 pose1` 搭配
  `think` / `think2` 表情。拼接错位的 `pose5` 已停用；原稿
  `arm5 + kowa` 的害怕动作改用 `xiayo2 pose6 + kowa`。

## 分文件接入结果

| 文件 | 原稿演出提示 | 已对齐 | 生成节点 | 主要内容 |
| --- | ---: | ---: | ---: | --- |
| `cp2_001` | 89 | 89 | 62 | 天空纵向运镜、池塘覆盖镜头、夏悠跳动、环境音 |
| `cp2_002` | 76 | 71 | 61 | 商店与咖啡馆黑场演出、人物走位、音效 |
| `cp2_003` | 80 | 80 | 67 | 水池 CG 差分、角色动作、场景与音乐 |
| `cp2_004` | 61 | 61 | 58 | 校园场景、角色走位、对话动作 |
| `cp2_005` | 44 | 44 | 46 | 实验室段落、覆盖镜头复位、立绘切换、音效 |
| `cp2_006` | 47 | 45 | 42 | 成年段落黑场替代、环境音与节奏 |
| `cp2_007` | 71 | 71 | 71 | 摩托 CG、萧可惊跳、进退场 |
| `cp2_008` | 86 | 86 | 78 | 趴桌 CG 1—11 差分、选择记忆 |
| `cp2_009` | 89 | 89 | 86 | 李宫娜预留立绘、多人站位和运动 |
| `cp2_010` | 91 | 91 | 85 | 刘守真预留立绘、多人场面和震动 |
| `cp2_011` | 42 | 42 | 41 | 夏悠便服姿势、场景切换 |
| `cp2_012` | 62 | 62 | 62 | 看书 CG 差分、背景覆盖复位、音效 |
| `cp2_013` | 72 | 70 | 65 | 火灾改编演出、循环烟雾、选择记忆、画面震动 |
| `cp2_014` | 54 | 54 | 48 | 条件分支、跨场景烟雾、角色动作 |
| `cp2_015` | 80 | 80 | 78 | 梦境与回眸 CG、音乐和转场 |
| `cp2_016` | 55 | 55 | 56 | 重逢 CG 差分、黑场复位、镜头节奏 |
| `cp2_017` | 45 | 41 | 41 | 条件分支、告白 CG 差分 |
| `cp2_018` | 33 | 33 | 33 | 结局 CG、音乐和停顿 |
| `cp2_019` | 5 | 5 | 5 | 章节收束 |
| **合计** | **1182** | **1169** | **1085** | 取消 CG 提示不生成节点 |

重建后的 3410 句对白中，2099 句为 ADV，1311 句为 NVL；NVL 按
`nvl clear` 与控制流边界拆为 258 个平衡块。当前 SakiEngine 追加的
10 句对白没有 Ren'Py 对应行，按其实际使用场景保留为 ADV。

“已对齐”少于原稿提示的 13 项来自有意生成零节点的取消 CG 和改写段落：
取消的咖啡馆 CG `shincg1` / `shincg2`、已改编的火灾 CG
`cg_shiokehomeinfire` / `shincg8`，以及移除的 `shincg10` 差分，不是
遗漏的可执行演出。

## 引擎层补齐

- `jump <label> if <variable> <true|false>`：通用条件跳转，复原
  `persistent.cp2_013_switch` 与 `persistent.cp2_important_switch`。
- 对话标签 `shake_N`：在原稿 `//s` 的准确字位触发对话框震动。
- `pose` + `pose-foreground`：复原身体、手臂、脸部的原始图层顺序。
- 原生 CG 合成器：底图常驻，仅替换透明差分，避免整张图片切换造成的
  闪烁和扭曲。
- `anime <name> loop keep` + `stop anime`：循环覆盖动画可跨对白与 CG
  保持，并在原稿指定边界显式退场。
- 循环 WebP 使用 Flutter 多帧流式解码，不再在章节载入时把数百张全屏
  帧全部预解码进内存。

## 待美术覆盖

- 李宫娜：`gonna2-pose1`—`pose3` 及 `gonna2-*` 表情。
- 刘守真：`syozen2-pose1`—`pose3` 及 `syozen2-*` 表情。

当前文件是全透明挂点，不会误用第一章造型。正式文件覆盖同名路径后，
已经写好的站位、差分与动画会直接生效。
